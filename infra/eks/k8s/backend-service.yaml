apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: opsboard
  labels:
    app: opsboard
    component: backend
spec:
  type: ClusterIP
  selector:
    app: opsboard
    component: backend
  ports:
    - port: 8080
      targetPort: 8080
