apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: opsboard
  labels:
    app: opsboard
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: opsboard
    component: frontend
  ports:
    - port: 8080
      targetPort: 8080
